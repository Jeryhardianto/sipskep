; Hanya dipasang di produksi (lihat compose.prod.yaml). Di dev tidak boleh:
; cookie_secure=1 bikin sesi hilang di http://localhost.
session.cookie_secure = 1
session.cookie_httponly = 1
session.cookie_samesite = Lax
expose_php = Off
